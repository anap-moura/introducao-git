git init - criar o repositório local

git remote add origin ' link do github'

git add .

git commit -m "mensagem de commit"

git push origin main

*agora estamos na nova branch

git checkout -b dev
(troca a branch e o -b CRIA uma nova branch)

